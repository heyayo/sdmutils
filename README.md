# SDM Utility Library
movers:
    swap(first, second) - Swaps the values of provided variables
    sort(Iterator one, Iterator two) - Sorts the provided container in ascending order, Container must have a valid Iterator